<!-- Page Breadcrumb -->
<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <div class="rst-breadcrumb">
        <a href="#"><span>Home</span></a>
        <span>></span>
        <a href="#"><span>Empresa</span></a>
      </div>
    </div>
  </div>
</div>
<!-- End Page Breadcrumb -->

<!-- Category Page Content -->
<section id="rst-catcontent-author">
  <div class="container">
    <div class="row">
      <div class="col-xs-12">
        <!-- Author zone -->
        <div class="rst-section-title rst-section-title-box">
          <h4><%= @announcer.fantasy_name %></h4>
        </div>
        <div class="rst-author-contact">
          <% if @announcer.avatar.present? %>
            <%= image_tag @announcer.avatar.url(:medium) %>
          <% else %>
            <img src="../images/user.png" alt="" />
          <% end %>
          <div class="rst-author-contactinfo">
            <p>
              <a><i class="fa fa-mobile"></i> <span>+48 122 33 66</span></a>
              <a><i class="fa fa-paper-plane"></i> <span><%= @announcer.email %></span></a>
              <a><span><i class="fa fa-location-arrow"></i><%= @personal_profile.street %>, <%= @personal_profile.neighborhood %>, <%= city_show(@personal_profile.city_id) %> - <%= state_show(@personal_profile.state_id) %></span></a>
              <a><i class="fa fa-lg fa-star" style='color: #EDA100;'></i> <span><%= @announcer.average('quality').avg %></span></a>
            </p>
          </div>
        </div>
        <p class="rst-author-excerpt"><%= @announcer.about_text %></p>
        <!-- End zone -->
      </div>
    </div>
  </div>
</section>
<%= media_section_banner(@announcer) %>
<!-- End Category Page Content -->
<section id="rst-newsandhot">
  <div class="container">
    <div class="row">
      <div class="col-xs-12">
        <div class="rst-section-title">
          <h4>Anúncios</h4>
        </div>
        <div class="rst-newsandhot-post">
          <div class="rst-newsandhot-post-content">
            <article>
              <div class="row">
                <% @announcer.ads.each do |ad| %>
                  <div class="rst-postpic col-sm-4 rst-postpic-img">
                    <%= link_to ad do %>
                      <%=  image_tag ad.avatar.url %>
                    <% end %>
                    <%= link_to ad, class: 'rst-postpic-cat' do %>
                      <span><%= ad.title %></span>
                    <% end %>
                  </div>
                <% end %>
              </div>
            </article>
          </div>
        </div>
      </div>
    </div>
    <%= media_section_gallery(@announcer) %>
  </div>
</section>
<section id="rst-latestcomments">
  <div class="container">
    <div class="row">
      <div class="col-xs-12">
        <div class="rst-section-title rst-section-title-box">
          <h4>Vagas de emprego</h4>
        </div> <!-- end section title -->
        <div class="row commentlist owl-carousel">
          <% @announcer.vacancies.each do |vacancy| %>
            <div class="col-sm-12 comment">
              <%= link_to vacancy do %>
                <div class="comment-avatar">
                  <%=  image_tag @announcer.avatar.url(:medium) %>
                </div>
              <% end %>
              <div class="comment-data">
                <%= link_to vacancy do %>
                  <%= @announcer.fantasy_name %>
                <% end %>
                <span class="comment-time"><i class="fa fa-clock-o"></i><%= vacancy.created_at %></span>
                <p class="comment-body"><%= vacancy.name %></p>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    </div>
    <div class="row">
        <div class="col-xs-12 rst-content-comment">
          <div class="rst-section-title rst-section-title-box">
            <h4>Avaliações desse anunciante</h4>
          </div>
          <ol class="commentlist">
            <% @announcer.quality_rates.each do |evaluation| %>
              <li class="comment">
                <div class="comment-container">
                  <div class="comment-avatar">
                    <% #if evaluation.rater.type.avatar.present? %>
                      <%#= image_tag evaluation.rater.type.avatar.url(:thumb) %>
                    <% #else %>
                      <%#= image_tag('user.png') %>
                    <%# end %>
                    <%= image_tag('user.png') %>
                  </div>
                  <div class="comment-data">
                    <div class="comment-header">
                      <a class="comment-author"><%= evaluation.rater.profile.name %></a>
                      <div class="rate-info">
                        <time class="comment-date"><i class="fa fa-clock-o"></i><%= evaluation.created_at %></time>
                        <%= rating_for_user @announcer, evaluation.rater, "quality", readonly: true %>
                      </div>
                    </div>
                    <div class="comment-body">
                      <p>
                        <%= evaluation.comment %>
                      </p>
                    </div>
                  </div>
                </div>
              </li>
            <% end %>
          </ol>
        </div>
        <div class="row rst-comments">
          <div class="container">
            <div class="col-xs-12">
              <div class="rst-section-title rst-section-title-box">
                <h4>Deixe sua avaliação</h4>
              </div>
            </div>
            <% if user_signed_in? %>
              <div class="col-sm-8">
                <div class="rst-getintouch-form" id='rate-form'>
                  <div class="alert hidden"></div>
                  Deixe sua avaliação: <%= rating_for_user @announcer, current_user, 'quality', star: 5 %> <br />
                  <%= text_area_tag :comment, @rating.try(:comment) || '', placeholder: 'Escreva um comentário', class: 'rst-pageinput' %>
                  <%= button_tag 'ENVIAR', id: 'rate-submit', class: 'rst-pagebutton' %>
                </div>
              </div>
            <% else %>
            logue!
            <% end %>
          </div>
        </div>
    </div>
  </div>
</section>
<!-- End Section Latest Comments -->

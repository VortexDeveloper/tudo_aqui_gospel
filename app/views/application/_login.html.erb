<%= form_for(resource, :as => resource_name, :url => session_path(:user)) do |f| %>
  <%= f.label 'Email' %>
  <%= f.text_field :email %>
  <%= f.label 'Senha' %>
  <%= f.password_field :password %>
  <%= f.submit 'Entrar', class: 'btn-login-facebook login-button  ' %>
  <%= render partial: 'devise/shared/links' %>
<% end %>
